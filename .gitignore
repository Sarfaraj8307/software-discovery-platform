node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# local build / QA scratch artifacts
*.log
qa-screenshots/

# Agent workspace state — local memory notes about the user and this project.
# Deliberately NOT published. Kept on disk; untracked from the index.
.workbuddy-ai/

# Graphify (codebase knowledge-graph tooling) — venv + generated artifacts
.venv-graphify/
graphify-out/
# Graphify published artifacts ARE committed so a fresh clone ships with the knowledge
# graph. They are generated output — re-run scripts/graphify-build.sh to refresh (it will
# then show these as modified; commit or not, per preference). The venv + raw graphify-out/
# below stay ignored.
# public/graphify/graph.html
# public/graphify/graph.json
